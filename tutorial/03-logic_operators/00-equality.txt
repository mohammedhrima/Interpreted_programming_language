/// to check if two strings ar the same you can do

str1 = "abcdef"
str2 = "abcdef"

// 1s method
res = str1 == str2
output("1st method: check if str1 is same as str2: ", res, "\n")

// 2nd method
res = str1 is str2
output("2nd method: check if str1 is same as str2: ", res, "\n")

x = 1
y = 12
output("check if x is equal to y: ", x == y, "\n")